<header class="row">
  <div class="col-sm-5">
    <h3><%= h @limit.name %></h3>
  </div>
</header>

<table class="table table-striped table-bordered">
  <tbody>
    <tr>
      <th><%= t('Type') %></th>
      <td><%= @limit.type_name %></td>
    </tr>
    <tr>
      <th><%= t('Size') %></th>
      <td><%= @limit.size %> / <%= @limit.interval %></td>
    </tr>
  </tbody>
</table>
